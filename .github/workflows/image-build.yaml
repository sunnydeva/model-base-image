name: Build and Push Docker Image

on:
  push:
    branches:
      - dev

jobs:
  build-and-push:
    runs-on: ubuntu-22.04 # stable, no containerd conflict

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: sirumallasunnydeva@gmail.com
          password: Satyam@1969
      - name: ls and pwd
        run: |
          ls -ltrh 
          pwd

      - name: Build and push Docker image
        run: |
          ls ./scripts -ltrh
          chmod +x ./scripts/image-build.sh
          bash ./scripts/image-build.sh
